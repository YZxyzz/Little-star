# 项目结构重组计划 (Project Reorganization Plan)

## 目标
将现有的原型验证代码 (`prototype/`) 整理为正式的工程化目录结构，以便后续长期开发维护。
我们将采用 **Monorepo (单体仓库)** 结构，明确区分 **应用端 (Apps)** 和 **服务端 (Services)**。

## 📁 目标目录结构

```text
Little-star/
├── apps/                  # 前端应用程序
│   └── mobile-web/        # [原 prototype/frontend-demo] 移动端 H5/PWA 应用
│       ├── src/
│       ├── package.json
│       └── ...
├── services/              # 后端微服务
│   └── ai-core/           # [原 prototype/ai-service] AI 核心服务 (Python)
│       ├── main.py
│       └── ...
├── docs/                  # [不变] 项目文档
│   ├── 07-app-design/
│   └── ...
├── prototype/             # [保留] 用于存放临时的、一次性的实验代码
│   └── (空)
└── README.md              # [更新] 项目入口说明
```

## 🔄 变更步骤

### 1. 创建核心目录
*   创建 `apps/` 目录。
*   创建 `services/` 目录。

### 2. 迁移前端项目
*   **Move**: `prototype/frontend-demo` -> `apps/mobile-web`
*   **Update**: 更新 `apps/mobile-web/package.json` 中的 `name` 为 `little-star-mobile`。

### 3. 迁移后端项目
*   **Move**: `prototype/ai-service` -> `services/ai-core`
*   **Update**: 确保 `services/ai-core` 环境配置独立。

### 4. 清理与文档
*   保留空的 `prototype/` 目录供未来使用。
*   更新根目录 `README.md`，添加各模块的启动指引。

## ✅ 验证计划

迁移完成后，我们将验证两个项目是否能在新位置正常运行：

### 验证前端
```bash
cd apps/mobile-web
npm install
npm run dev
# 预期：浏览器能正常打开前端页面
```

### 验证后端
```bash
cd services/ai-core
# (如果需要) python -m venv venv && source venv/bin/activate
pip install -r requirements.txt
python main.py
# 预期：服务正常启动，无路径错误
```
